from __future__ import annotations

import importlib.util
from pathlib import Path


def _load_common_module():
    repo_root = Path(__file__).resolve().parents[1]
    module_path = repo_root / "tools" / "append_thesis_ch2.py"
    spec = importlib.util.spec_from_file_location("append_thesis_ch2", module_path)
    if spec is None or spec.loader is None:
        raise RuntimeError(f"Unable to load module: {module_path}")
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)
    return module


def make_chapter5_content() -> dict[str, object]:
    toc = [
        ("chapter", "5 系统实现 .......................................................... 182"),
        ("section", "5.1 系统实现概述 .................................................. 182"),
        ("section", "5.2 前端核心功能实现 .............................................. 186"),
        ("subsection", "5.2.1 登录认证与用户会话实现 ....................................... 186"),
        ("subsection", "5.2.2 统一工作台与多模式交互实现 ................................... 191"),
        ("subsection", "5.2.3 文件上传、OCR 与会议纪要前端实现 ............................. 197"),
        ("subsection", "5.2.4 写作中心、分享与后台界面实现 ................................. 203"),
        ("section", "5.3 后端核心服务实现 .............................................. 209"),
        ("subsection", "5.3.1 应用启动、路由装配与中间件实现 ............................... 209"),
        ("subsection", "5.3.2 聊天服务与上下文编排实现 ..................................... 215"),
        ("subsection", "5.3.3 历史会话、分享与认证接口实现 ................................. 221"),
        ("section", "5.4 智能能力模块实现 .............................................. 227"),
        ("subsection", "5.4.1 文档解析与检索增强问答实现 ................................... 227"),
        ("subsection", "5.4.2 数据库智能问答实现 ........................................... 233"),
        ("subsection", "5.4.3 OCR 与会议音频处理实现 ....................................... 239"),
        ("subsection", "5.4.4 智能审单流程实现 ............................................. 246"),
        ("subsection", "5.4.5 报告、邮件与 PPT 生成实现 ................................. 252"),
        ("section", "5.5 管理与运行支撑实现 ............................................ 259"),
        ("subsection", "5.5.1 角色治理与后台管理实现 ....................................... 259"),
        ("subsection", "5.5.2 运行时资源、安全机制与可维护性实现 ......................... 265"),
        ("section", "5.6 本章小结 ..................................................... 271"),
    ]

    sections = [
        {
            "title": "系统实现概述",
            "level": "section",
            "paragraphs": [
                "第四章完成了系统的总体设计与关键模块设计，而第五章的任务，是从工程落地角度说明这些设计如何在当前项目中转化为可运行的代码系统。对于企业智能办公助手而言，系统实现并不是将若干页面、接口和模型调用简单拼接在一起，而是要把统一工作台、后端服务骨架、数据库与对象存储、多模态处理能力、规则治理机制以及后台管理能力组织成能够长期运行、持续迭代并适应企业场景的完整工程。正因如此，本章强调的重点是“真实实现路径”，而不是概念层面的重复阐述。",
                "从项目代码结构来看，系统实现具有明显的全栈协同特征。前端以 React 构建统一工作台和多模式交互界面，后端以 FastAPI 组织路由与能力装配，数据层依托 Supabase/PostgreSQL 与对象存储提供结构化和非结构化资源承载，智能能力层进一步整合大语言模型、检索增强、数据库问答、OCR、会议音频转写、审单分析以及报告与 PPT 生成等服务。换言之，本系统的实现逻辑并非“单模型驱动前端”，而是“工作台驱动多服务编排”。",
                "在具体实现策略上，项目遵循了模块化与渐进集成并重的工程思路。前端将登录、仪表盘、模式面板、审单工作区、写作入口和共享页面拆分为独立组件，同时通过统一会话与状态对象维持用户体验连续性；后端则通过主程序装配、分路由组织、可选模块加载和后台线程处理，保证复杂能力可以以较低耦合接入系统；数据层则围绕会话历史、知识切片、共享快照、音频对象和运行时资源建立持久化与清理机制。这一实现策略保证了系统在面对不同任务场景时仍能保持清晰边界。",
                "因此，本章将围绕前端核心功能实现、后端核心服务实现、智能能力模块实现以及管理与运行支撑实现四个层面展开说明，重点分析项目中的关键文件、主要处理链路和实现机制，进而展示 Enterprise Intelligent Office Agent 2.0 是如何从设计方案转化为具有实际业务支撑能力的软件系统的。",
            ],
        },
        {
            "title": "前端核心功能实现",
            "level": "section",
            "paragraphs": [
                "前端实现是系统与用户之间最直接的接触面。本项目并没有把不同智能能力分散为彼此独立的多个页面，而是以统一工作台为中心，将登录认证、聊天交互、知识问答、数据库分析、OCR、会议纪要、审单、写作和 PPT 生成入口等功能纳入同一界面体系。这样做的根本原因在于，企业办公任务往往具有明显的上下文连续性，若前端以孤立页面方式实现，就会造成任务链路割裂和信息重复输入。",
                "在工程组织上，前端主要围绕 DashboardPage、ModePanel、AuditWorkspace、WritingEntryHub、ShareModal、Sidebar 以及多个 API 模块展开。DashboardPage 负责统一状态编排和会话控制，ModePanel 负责按模式呈现不同工具能力，AuditWorkspace 和写作中心承接垂直业务界面，API 封装模块则负责与后端建立稳定的数据交互。可以说，前端实现本质上是一个面向多模式任务的交互调度系统，而不仅是单纯的聊天页面。",
            ],
        },
        {
            "title": "登录认证与用户会话实现",
            "level": "subsection",
            "paragraphs": [
                "前端登录认证的实现以统一请求封装和会话续期策略为基础。项目在 apiClient.js 中对 API_BASE_URL、访问令牌、刷新令牌和“记住登录”时间窗进行了集中管理，并通过 executeRequest 与 apiClient 两层封装把普通接口调用、鉴权头附加、401 重试和错误统一处理收敛到同一位置。这样的实现方式避免了各个页面重复处理授权逻辑，提高了前端代码的一致性和可维护性。",
                "在会话续期机制上，前端优先通过 /api/auth/refresh 调用后端刷新接口更新访问令牌；若刷新令牌在 Supabase SDK 内已轮换，则先同步浏览器侧 Supabase 会话，再使用新的 refresh_token 重试后端刷新；若后端路径不可用，前端还可以直接调用 Supabase 的 refreshSession 完成续期。这种多层回退逻辑使系统能够在长期使用工作台的过程中尽量避免频繁跳回登录页，较好地适配了企业办公中“长时在线、间歇操作”的使用模式。",
                "后端对应的认证实现集中在 auth_router.py。该模块不仅提供登录、注册、短信验证码和刷新令牌相关接口，还维护了短信登录临时令牌的内存加磁盘缓存，并通过 runtime_storage 中统一的缓存文件路径保存 SMS_TOKEN_CACHE_FILE。由于系统支持短信口令和 Supabase 认证共存，因此认证实现不能只围绕单一 JWT 流程展开，而要兼顾临时口令映射、失效清理、磁盘持久化和用户资料同步等问题。",
                "用户资料同步方面，auth_router.py 在用户登录或注册后会调用 _sync_profile，将邮箱、角色、状态、部门和岗位等信息写入 profiles 表；前端则通过统一鉴权态驱动页面加载、头像展示和权限判断。与此同时，后端还支持头像上传到 avatars 存储桶，并通过管理员客户端与匿名客户端的分层使用保持安全边界。由此可见，系统的认证实现并不是简单返回一个 token，而是已经与用户资料、角色信息和资源存储形成联动。",
                "前端登录认证实现的另一个特点，是其与后续多模式页面并没有明显割裂。无论是历史会话页、管理后台页还是分享页，实际都依赖同一套令牌与用户状态策略。这意味着登录模块不仅是系统的入口，更是所有业务能力可达性的前置条件。通过将认证、会话续期、角色资料和统一请求封装结合起来，系统建立起了后续复杂办公场景稳定运行的前端基础。",
            ],
        },
        {
            "title": "统一工作台与多模式交互实现",
            "level": "subsection",
            "paragraphs": [
                "统一工作台的核心实现集中在 DashboardPage.jsx。该页面不是传统意义上的简单聊天容器，而是负责维护当前会话标识、会话列表、模式状态、上传文件队列、OCR 状态、会议状态、审单状态、共享弹窗状态以及写作入口状态等多个关键对象。由于不同模式之间既相互独立又共享同一会话语义，因此 DashboardPage 实际承担了前端状态调度中心的角色，是系统实现中最关键的页面模块之一。",
                "在工作台交互上，系统通过 Sidebar 与 MobileSidebar 维护用户会话入口，通过消息列表和输入区承接持续对话，通过建议问题、来源面板和模式标识增强用户对当前任务语境的理解。用户既可以继续已有历史会话，也可以创建新的任务会话，前端还支持会话重命名、删除、置顶和分页拉取。这种实现方式说明，会话在本项目中并非一次性消息列表，而是承载用户长期办公任务的重要对象。",
                "多模式交互的具体呈现由 ModePanel.jsx 负责。该组件根据当前模式动态展示数据库分析、OCR、会议纪要、审单和其他辅助界面，并通过 React.lazy 延迟加载 AuditWorkspace 等体量较大的子模块，从而平衡首屏加载性能与功能完整度。模式面板中还集成了音频播放器、文档结果展示、审单配置面板和高亮文本浏览等组件，使不同任务模式可以在统一的视觉框架下获得差异化的交互表达。",
                "前端在多模式实现中非常强调“上下文连续”。例如，用户在知识库问答中上传的文件可以作为当前会话上下文继续提问；在数据库模式中获取的数据结果可以直接作为写作输入；在会议模式中完成转写后，又能在新会话中继续提炼行动项和纪要内容。为了实现这种连续性，DashboardPage 中围绕会话、附件和模式的状态更新逻辑被设计得较为细致，相关处理不只是界面切换，更是上下文装配的前端表达。",
                "工作台实现还体现出一定的性能意识。项目使用 React Router 组织页面级导航，使用懒加载降低大型模式组件的初始开销，并通过局部状态更新而非全局状态容器避免不必要的重渲染。对于智能办公工作台这类长会话、多交互、富状态页面而言，状态更新粒度是否合理直接影响实际体验，因此当前实现具有较强的工程针对性。",
                "综合来看，统一工作台与多模式交互实现使系统真正具备了“一个入口承接多种办公任务”的产品形态。前端不再只是等待后端结果的被动展示层，而是承担了任务组织、上下文维系和交互语义表达三重职责，从而为整个平台的多场景协同提供了直观而稳定的使用入口。",
            ],
        },
        {
            "title": "文件上传、OCR 与会议纪要前端实现",
            "level": "subsection",
            "paragraphs": [
                "文件上传实现贯穿知识库、OCR、会议纪要和审单等多个模式，因此前端并未将其写成某个场景下的临时逻辑，而是以包装对象、进度状态和统一处理函数的方式组织。DashboardPage.jsx 中的 uploadAndVectorizeFiles、processFiles 等方法负责把原始 File 对象转换为携带状态、进度和预览信息的包装对象，再根据当前模式分流到知识库上传、OCR 识别、音频转写或审单任务接口。这样的实现方式保证了同一上传机制可以被多个业务场景复用。",
                "在知识库上传场景中，前端通过 FormData 将文件提交到 /api/documents/upload，并配合上传结果轮询接口获取向量化状态和预览文本。每个待上传文件都有独立的进度条、成功失败标识和错误信息，这使用户能够感知文档进入知识库的真实过程，而不是只在最终收到一条“上传成功”提示。对于多文件上传场景而言，这种细粒度反馈是保证可用性的关键。",
                "OCR 场景的前端实现更强调可视化和二次处理。用户提交图片或 PDF 后，DashboardPage 会向 /api/ocr/recognize 发起请求，同时设置超时控制，避免超大图像在等待过程中造成界面失去响应。识别成功后，前端不仅展示 OCR 文本，还保存页级数据、行级数据、原文件 URL、JSON 结果和服务器侧路径，并可将识别结果写入历史会话。这样的实现说明 OCR 在项目中不是一次性接口调用，而是一个带有保存、预览、复查和继续编辑能力的完整交互子流程。",
                "会议纪要前端实现则围绕音频上传、后台轮询和结果承接展开。系统在会议模式下会优先尝试即时转写，若不能即时返回，则通过 /api/voice/transcribe 提交异步转写任务，再轮询 /api/voice/result/{task_id} 获取结果。前端同时记录音频文件路径，供后续播放器回放使用。值得注意的是，会议音频上传完成后，前端会为转写结果生成新的会话标识并切换到新页面，避免新音频内容继续附着在旧会话底部，保证了会议材料作为独立任务上下文的完整性。",
                "ModePanel.jsx 中的音频播放器组件进一步增强了会议纪要场景的可用性。播放器实现了播放、暂停、拖动定位、倍速切换、音量控制和静音操作，并将当前时间、总时长和播放进度以可视化方式呈现。这样一来，用户在查看转写文本时仍可回听原始录音，系统也因此具备了“文本结果 + 原始证据”并存的交互特征。",
                "总体来看，文件上传、OCR 与会议纪要前端实现已经形成了较为完整的多模态内容接入链路。不同介质虽然在后台处理方式上存在差异，但前端都围绕统一上传入口、明确状态反馈、结果保存和继续利用这几个目标组织交互，从而为后端的知识处理、识别和转写能力提供了稳定的人机交互承载层。",
            ],
        },
        {
            "title": "写作中心、分享与后台界面实现",
            "level": "subsection",
            "paragraphs": [
                "写作中心的实现采用了“一级入口 + 二级能力”的组织方式。WritingEntryHub.jsx 作为独立入口组件，首先让用户在“写作助手”和“PPT 生成”之间做一级选择，再分别进入对应的子界面。这种实现思路避免了在统一工作台中直接堆叠过多表单和配置项，使写作类能力在保持统一风格的同时，仍具备相对独立的操作空间。",
                "在写作助手场景中，前端会把当前会话上下文、用户输入和模式信息交给后端写作服务生成邮件、报告或其他文本草稿；在 PPT 生成场景中，前端则进入独立的 PPT 工作流，支持主题输入、模板选择、任务提交、状态轮询和文件下载。由于 PPT 生成往往比普通问答耗时更长、配置更多，单独设置入口不仅降低了界面复杂度，也有利于用户理解不同写作任务之间的差异。",
                "分享功能的前端实现主要由 ShareModal 与共享页面构成。用户可以在当前会话上发起分享请求，前端通过 shareApi.createShare 调用后端生成带时效与权限参数的分享链接，并在弹窗中展示可复制地址。打开共享链接后，共享页面再通过读取后端快照结果恢复被分享的会话内容。这样一来，系统不仅支持“个人会话内部持续使用”，也支持将成熟结果转化为可传播的知识资产。",
                "后台界面实现方面，前端为管理员与审单人员保留了独立工作区。AuditWorkspace 在用户侧和管理侧都承担重要职责，其前端界面能够展示文件信息、任务状态、异常项、得分、ERP 动作按钮和结果摘要；管理员页面则进一步承接用户管理、规则管理、知识库管理与审计记录查看等组织治理功能。前端通过路由、角色状态与局部配置面板将这些后台能力与普通工作台区分开来，避免对一般用户造成干扰。",
                "写作中心、分享与后台界面的实现共同说明，本项目的前端已经超越了“聊天问答器”的范畴，而开始具备内容产出、结果传播和组织治理三类典型企业软件特征。正是这些界面的存在，才使系统能够从个人效率工具进一步发展为组织级办公平台。",
            ],
        },
        {
            "title": "后端核心服务实现",
            "level": "section",
            "paragraphs": [
                "如果说前端工作台负责承载用户侧任务流程，那么后端核心服务实现则负责把这些前端动作转化为可执行、可控制、可追踪的业务链路。本项目的后端并未将所有能力揉进单一脚本，而是围绕主程序装配、能力路由、上下文管理、历史持久化和后台线程处理形成了一套模块化服务结构。不同服务既共享统一框架，又能在边界清晰的前提下独立演进。",
                "从工程实际看，后端的关键价值并不只在于“提供接口”，更在于对模型、数据库、文件、多模态处理和组织治理能力进行编排。也就是说，后端承担的是统一能力网关和任务调度中枢的职责。下面将分别从应用启动与装配、聊天服务与上下文编排、历史会话与认证共享机制三个方面展开说明。",
            ],
        },
        {
            "title": "应用启动、路由装配与中间件实现",
            "level": "subsection",
            "paragraphs": [
                "主程序 main.py 是整个后端服务的装配入口。系统启动时会先加载 .env 与 .env.local 配置，再调用 runtime_storage 中的 ensure_runtime_layout 和 migrate_legacy_runtime_files 建立统一运行时目录，并完成旧版 OCR、音频、审单和缓存文件向 .runtime 目录体系的迁移。这样的启动流程说明，系统并不是简单监听 HTTP 端口，而是在服务真正对外可用之前先完成运行环境自校准。",
                "在能力装配方面，main.py 使用“可选导入 + 条件注册”的策略组织认证、聊天、审单、管理后台、决策、文档处理、语音处理、分享和 PPT 生成等模块。若某些依赖未就绪，系统会打印模块不可用日志，但其他模块仍可继续启动。这种实现方式较为务实，既提高了开发联调和环境迁移时的容错性，也避免某个边缘能力因依赖问题拖垮整个系统。",
                "应用启动后还会自动执行两类重要的后台初始化任务。第一类是运行时资源清理线程，系统在 startup 事件中调用 cleanup_runtime_files 和 start_runtime_cleanup_loop，对 OCR 结果、临时音频、审单状态与缓存文件按生命周期进行清理；第二类是模型与能力预热线程，main.py 会根据环境变量决定是否预热大语言模型、嵌入模型和决策缓存，从而降低首次请求延迟。这些实现细节体现了系统对长期运行体验的关注。",
                "中间件实现同样是后端核心服务的重要部分。系统在 log_requests 中间件中记录非健康检查和非轮询接口的请求路径、响应状态和耗时，便于开发与运维阶段进行接口级问题定位；在 enforce_user_status 中间件中，又结合缓存和用户资料查询结果，对账号状态进行快速检查和限制。这种“日志可观测 + 用户状态可控”的中间件组合，为企业场景中的接口治理提供了基础支撑。",
                "主程序还承担了一部分公共文件接口职责。例如 OCR 上传后的文件会通过运行时静态目录对外暴露，语音转写、文档上传等业务接口虽然由独立模块处理，但其入口统一挂载在 main.py 中，这使前端只需围绕 /api 命名空间与一个后端服务交互即可。统一入口的实现降低了接口发现成本，也便于后续扩展新的能力路由。",
                "总体来看，应用启动、路由装配与中间件实现构成了后端服务的骨架层。正是这一层保证了聊天、知识库、数据库、语音、OCR、审单和 PPT 等异构能力能够在同一后端进程中被有序装配、按需启用并稳定暴露给前端调用。",
            ],
        },
        {
            "title": "聊天服务与上下文编排实现",
            "level": "subsection",
            "paragraphs": [
                "聊天服务的核心实现集中在 chat_router.py。该模块并没有将用户输入直接转发给某个固定模型，而是首先识别当前模式、会话状态、历史记录和附件上下文，再决定后续链路应走通用聊天、知识库检索、数据库问答、OCR 继续处理、会议纪要继续追问或其他任务路径。由此可见，本项目中的聊天服务本质上是一个基于模式的任务编排器，而不是单纯的模型调用代理。",
                "为了让多轮交互保持连贯，chat_router.py 中实现了较为完整的上下文组装机制。系统会读取最近历史消息、上下文记录、模式状态以及必要的元信息，并对文本进行清洗、截断和重排，再交由下游模型或工具链使用。对于会议转写、OCR 结果和数据库查询结果这类体量较大的上下文，模块还会进行长度控制和内容提炼，避免上下文无限膨胀影响推理稳定性。",
                "聊天服务对不同模式的上下文边界控制十分明确。在知识库模式下，系统会调用 search_user_documents 检索相关文档片段；在数据库模式下，会围绕表白名单和 SQL 结果组织回答；在普通模式下，则更强调通用对话和已有会话历史。这样的模式分流避免了所有请求都混用同一类证据来源，保证了结果在语义上和事实来源上都具有相对稳定的边界。",
                "在响应输出方面，后端使用流式响应机制将模型生成过程逐步返回前端。对于报告撰写、长摘要和复杂解释这类任务，流式输出显著改善了等待体验，也让前端可以边接收边渲染。更重要的是，流式聊天实现与历史记录写入并不冲突，系统在生成完成后仍会将用户问题和助手回复以结构化形式持久化到历史表中，实现“实时体验”和“结果留痕”的统一。",
                "chat_router.py 还承担了一部分上下文元事件记录职责。例如，当某个模式写入新的上下文、当会话需要保存特殊元数据、或者当数据库结果需要转入后续写作阶段时，系统会通过历史记录和上下文保存函数写入专门的 context 或 meta 类型消息。这样的实现细节虽然不直接体现在前端界面上，却是保证系统在多模式、多轮次条件下仍能维持上下文完整性的关键。",
                "因此，聊天服务与上下文编排实现可以被视为整个系统的逻辑中枢。无论用户使用的是知识库、数据库、OCR 还是会议纪要模式，最终都要回到这一层完成消息组织、上下文控制、模型输出和结果回写。这也是统一工作台能够承接复杂企业办公任务的根本原因。",
            ],
        },
        {
            "title": "历史会话、分享与认证接口实现",
            "level": "subsection",
            "paragraphs": [
                "历史会话实现集中在 history_manager.py。该模块不仅提供新增历史、读取会话、分页获取历史消息、列出会话列表和重命名会话等基础能力，还专门加入了对本地数据库迁移漂移问题的自修复处理。例如，模块会自动修复 history.id 的自增序列缺失问题，并创建基于 user_id、session_id、created_at 的查询索引。这些实现细节说明，系统在工程层面已经考虑到真实运行环境中的数据库演化与兼容问题。",
                "会话标题管理同样是历史实现的重要组成部分。history_manager.py 会根据首条用户消息或上下文消息自动生成简短标题，并针对 voice_context、ocr_context、audit_context 等不同类型上下文设置默认标题，从而让用户在侧栏中能够快速辨认不同会话的语义主题。会话标题并非简单的界面装饰，而是长期办公过程中组织任务的重要索引。",
                "分享能力的后端实现集中在 share_manager.py。该模块会自动创建 share_links 与 share_snapshots 两张数据表及其序列、索引和外键约束，并使用随机令牌加 SHA-256 哈希方式生成对外分享标识。创建分享时，系统并不是直接暴露全部原始历史记录，而是通过截断、头尾保留和快照写入机制控制分享体积和敏感性。这种实现既保留了知识传播价值，也兼顾了企业场景下的结果边界控制。",
                "认证接口层面，auth_router.py 负责登录、注册、短信验证码、刷新令牌、头像上传和用户资料同步等能力。其中，短信登录临时会话通过内存与磁盘双重缓存维护，既支持当前进程快速读取，又能在服务重启后保留有效映射；头像上传则借助对象存储桶完成长期保存；用户资料同步又保证了 profiles 表中角色、部门和岗位信息的持续更新。这种实现表明，认证并不是独立存在的账户子系统，而是与用户资料、会话使用和组织治理能力密切耦合。",
                "前端围绕这些接口进一步实现了历史分页、重命名、删除、分享生成和共享页读取等交互能力，使历史与分享不再停留在后端存储层，而真正成为用户可操作的办公资产。这说明历史会话、分享与认证接口共同构成了系统的“持续使用层”，把一次性对话升级为可保存、可传递、可追踪的知识生产过程。",
                "综上所述，历史会话、分享与认证接口实现保证了系统具备组织长期知识与用户状态的能力。若缺少这一层，系统即便具备较强的智能推理能力，也仍然只是一次性交互工具；而通过该层的实现，平台得以逐步沉淀为一个拥有用户身份、任务历史和共享机制的企业智能工作空间。",
            ],
        },
        {
            "title": "智能能力模块实现",
            "level": "section",
            "paragraphs": [
                "智能能力模块是本项目区别于普通管理系统和普通聊天系统的关键所在。它们直接决定平台是否能够处理企业知识、结构化经营数据、图像与音频资料、审单流程以及成果生成任务。当前项目中的智能能力并不是孤立分布的若干接口，而是围绕知识、数据、多模态和流程输出形成了若干相互协同的实现链路。",
                "从实现角度看，这些能力模块都建立在“程序约束 + 模型理解 + 结果回写”的统一思想之上。换句话说，系统并不将所有任务完全交给模型自由发挥，而是通过文档切分、向量检索、表白名单、OCR 引擎、转写任务、规则引擎和模板注册等程序性手段，为模型和用户都建立可控边界。下面将分别从文档检索、数据库问答、OCR 与会议音频、智能审单以及内容生成五个模块展开说明。",
            ],
        },
        {
            "title": "文档解析与检索增强问答实现",
            "level": "subsection",
            "paragraphs": [
                "文档解析与检索增强问答的核心实现集中在 documents_processing.py。系统首先通过 load_documents 对 PDF、DOCX、TXT 等不同格式文件进行统一解析，并根据文件扩展名调用相应的解析策略。对于 Word 文档，模块会借助专门的读取路径提取文本；对于其他文本材料，则进一步进入统一切分与向量化流程。这样的实现方式保证了知识库入口具备较好的格式适应性。",
                "在文本切分阶段，系统实现了自适应切片策略。_adaptive_split_documents 会根据文档长度、结构特征和文件类型调整切片大小、重叠比例和分段方式，从而尽量保留合同、表格说明和长文本中的语义连续性。相较于使用固定窗口硬切分，自适应切片更适合企业文档这种结构差异较大、信息密度不均匀的材料，也更有利于后续检索时召回真正相关的片段。",
                "完成切分后，系统会使用本地嵌入模型对文本片段执行向量化，并把正文、向量和元数据一起写入 documents 表。元数据中通常包含来源文件名、用户标识、页码、标签和更新时间等信息，这使知识库中的每条记录都不仅是一个文本片段，更是一个可过滤、可追踪、可进行权限隔离的语义单元。对企业场景而言，这种细粒度元数据组织是保证知识库可治理性的前提。",
                "文档上传并非完全同步阻塞在前端请求中。系统还实现了 document_upload_tasks.py，用于在需要时以后台任务方式组织文档向量化和结果查询。前端通过上传接口和结果轮询接口获取处理状态，后端则根据 replace_existing 等参数决定是否清空旧文档并重建索引。这种实现使知识库构建既保留了工程灵活性，也避免大文件处理时占满主请求链路。",
                "在检索阶段，search_user_documents 会优先调用 Supabase 侧的 match_documents RPC 进行向量相似度召回，并在需要时通过本地打分回退路径返回最相关文档片段。召回结果随后与用户问题和会话历史共同进入聊天链路，由模型基于证据生成回答。来源面板再把文件名、片段和出处反馈给用户，形成“语义召回 - 模型归纳 - 来源展示”的完整闭环。",
                "因此，文档解析与检索增强问答实现并不是把上传文件简单存入数据库，而是通过解析、切分、嵌入、元数据管理、任务调度和向量检索等多个步骤，将静态文档转化为可持续服务于企业问答与写作的知识基础设施。这也是系统能够在私域知识场景中提供高质量回答的根本原因。",
            ],
        },
        {
            "title": "数据库智能问答实现",
            "level": "subsection",
            "paragraphs": [
                "数据库智能问答实现主要集中在 database_manager.py。该模块首先根据 Supabase PostgreSQL 配置构建连接字符串，再通过 langchain_community 的 SQLDatabase 及自定义逻辑组织自然语言到 SQL 的处理链路。与通用聊天不同，数据库问答直接关系到企业经营事实，因此系统在实现上首先考虑的不是生成自由度，而是约束与安全。",
                "为保证可控性，模块预先定义了严格的 ALLOWED_TABLES 白名单，仅允许 company_info、departments、roles、employees、customers、suppliers、products、inventory、orders、order_items 和 purchases 等 11 张业务表参与查询。同时，代码中进一步定义了表结构摘要、表关系、关键词映射和统一规范提示词，使模型在生成 SQL 时能够围绕实际业务语义进行推理，而不是臆造字段和表名。这样的实现显著降低了数据库问答的风险。",
                "SQL 生成之后，系统还会通过 _clean_sql、_is_safe_readonly_sql 和 _validate_table_whitelist 等函数执行二次清洗和只读校验。凡是包含 INSERT、UPDATE、DELETE、DROP、ALTER 等危险语句的输出都会被拦截，包含未授权表名的查询也会直接判为非法。换言之，模型只负责在约束范围内给出候选 SQL，而真正决定是否可执行的是后端程序逻辑。这种“模型生成 + 程序守门”的实现方式符合企业场景对事实系统访问的基本要求。",
                "为提升查询性能与连续使用体验，database_manager.py 还实现了 SQL 缓存、结果缓存和摘要缓存机制，并对时间范围、结果条数和本地上下文长度设置了多个参数阈值。当用户围绕同一主题连续追问时，系统能够利用近期状态和缓存结果减少重复计算，提高数据库模式的响应稳定性。由此可以看出，数据库问答不仅仅是一个功能点，而是具备一定运行优化意识的子系统。",
                "数据库执行完成后，系统还会把结构化结果重新组织成用户可理解的自然语言说明，再交由前端工作台展示。这意味着数据库智能问答实现既包含事实查询链路，也包含结果解释链路，从而把传统 SQL 工具面向技术人员的能力转化为面向业务用户的自然语言分析服务。正因为如此，本模块成为系统进入经营分析场景的重要支撑。",
            ],
        },
        {
            "title": "OCR 与会议音频处理实现",
            "level": "subsection",
            "paragraphs": [
                "OCR 实现集中在 ocr_manager.py。该模块在启动时会优先尝试加载 PaddleOCR-VL 视觉语言模型，以适应复杂版面和图文混排文档；若相关依赖不可用，则自动回退到标准 PaddleOCR 引擎。考虑到项目运行环境包含 Windows 本地部署场景，代码中还专门加入了 DLL 路径补齐、OMP 冲突规避和旧版 Paddle 接口兼容等处理。这些实现细节说明 OCR 模块已经从实验性质调用发展为面向真实环境的工程能力。",
                "在输入处理层，OCR 模块通过 pdf2image 将 PDF 转换为图像，再与普通图片一起统一进入识别流程。识别结果不仅包括纯文本，还可能包含页级组织、行级位置、结构化 Markdown 和引擎信息。对于用户而言，这意味着 OCR 结果既可直接用于阅读，也可进一步用于结构化抽取、保存历史或进入后续问答流程。这样的多层结果组织使 OCR 模块在系统中具备了比“输出一段文本”更高的复用价值。",
                "系统还在 OCR 之后接入了结构化处理能力。通过 parse_ocr_content 与 save_ocr_record 等模块，识别得到的文本和结构化字段可以进一步保存到数据库或作为上下文写入会话。这一实现使 OCR 不再只是附件阅读辅助工具，而能真正转化为后续录入、审单和知识积累的输入来源，从而增强了多模态资料与平台其他能力之间的连通性。",
                "会议音频处理实现则主要集中在 voice_files_processing.py 与 voice_manager.py。当前系统已将语音任务简化为进程内状态管理，不再依赖 Redis 或 RQ，而是通过 TRANSCRIPTION_TASKS 维护任务状态，通过后台线程执行本地分片转写。上传的音频首先可以写入 voice_uploads 对象存储桶，随后由后台线程直接从上传字节或存储字节中读取音频数据，进入本地分片转写流程。这样的实现既降低了部署复杂度，也适应了本地 Supabase 存储环境。",
                "voice_manager.py 中的转写实现会先将原始音频转换为适合语音识别的格式，再按片段调用百度语音能力，最后将多个片段结果拼接为完整会议文本。与依赖公网 URL 的长音频拉取模式相比，本地分片转写的实现更符合当前项目的私有化部署条件，也更能控制文件可达性和异常处理路径。转写完成后，结果被写回任务状态，再由前端轮询接口读取并放入新的会议会话页面中。",
                "因此，OCR 与会议音频处理实现共同承担了系统的多模态内容接入职责。前者把图像、票据和 PDF 转化为可阅读、可结构化的文本资源，后者把语音会议转化为可总结、可追问的文本上下文。两者都不是孤立功能，而是整个企业智能办公工作流中的上游输入层。",
            ],
        },
        {
            "title": "智能审单流程实现",
            "level": "subsection",
            "paragraphs": [
                "智能审单是本项目最具业务垂直特征的模块之一，其核心实现集中在 audit_router.py、audit_pipeline.py 以及相关后台页面。前端提交文件和文档类型后，audit_router.py 会创建审单任务并提供状态查询与 ERP 回写接口，而真正复杂的处理则由 audit_pipeline.py 负责。可以说，审单模块在系统中承担的是“AI 能力 + 规则流程 + 业务动作”融合实现的代表性角色。",
                "audit_pipeline.py 在初始化时会借助 runtime_storage 建立专门的审单状态目录，并维护作业状态、案件状态和 ERP 队列状态等多个本地持久化路径。这意味着审单流程并不完全依赖数据库中的某一张表，而是同时通过本地 JSON 状态和数据库记录构建更稳健的过程管理。对于高敏感业务而言，这种双重状态保留方式能够提高过程可恢复性和结果留痕能力。",
                "具体处理链路上，审单任务通常会经历文件保存、OCR 或文档解析、字段抽取、文档子类型识别、规则校验、异常发现、AI 评估、案件完整度计算和结果汇总等多个阶段。系统在代码中定义了丰富的文档别名、风险等级、工作流状态和案件文档集合，说明审单实现并不是只生成一段意见文本，而是要围绕真实业务票据和文件材料构建较完整的分析过程。",
                "在结果计算上，audit_pipeline.py 会汇总规则发现、AI 评估和 ERP 上下文，生成 audit_score、风险等级、摘要、下一步动作建议和案件文档视图，并把结果持久化到 audit_results、audit_jobs 以及相关明细记录中。随后，admin_router.py 又会进一步把这些结果组织为适合后台筛选、搜索、复核和列表展示的审单记录。这意味着审单模块不仅面向前台单次结果返回，更已经具备组织级审核记录管理能力。",
                "审单流程的另一项重要实现，是 ERP 动作写回。系统支持 queue 与 direct 两种同步模式，并通过 ERP 适配层封装具体写回行为。当审单结果完成后，用户或审核人员可以触发批准、驳回或补充材料等动作，系统再将这些动作排入 ERP 队列或直接推送给适配器。这种实现使审单功能不再止于“辅助判断”，而能够向实际业务系统动作延伸。",
                "由此可见，智能审单流程实现体现出本项目在企业场景中的深度落地能力。它把文件解析、规则核验、模型分析、状态管理和业务系统对接组织成了一条闭环处理链路，使系统从一般性的智能辅助平台进一步发展为可嵌入业务流程的专业工具。",
            ],
        },
        {
            "title": "报告、邮件与 PPT 生成实现",
            "level": "subsection",
            "paragraphs": [
                "内容生成实现主要由 report_email_manager.py、前端写作入口以及 presentation_router.py 等模块共同构成。与普通聊天问答不同，报告、邮件和 PPT 生成强调的是“面向成果输出”，因此实现上更关注任务意图识别、上下文重组、结构化输出以及后续下载与分享等环节。这样的定位使内容生成模块成为系统中承接最终办公产物的重要能力层。",
                "在报告与邮件生成方面，report_email_manager.py 会先分析用户输入和历史上下文，判断当前意图更偏向报告、邮件还是 PPT 内容，再根据不同意图构造提示词和流式输出逻辑。模块不仅考虑了当前轮输入，还会对近期历史进行格式化，以保证生成内容与现有任务背景一致。这种实现方式说明，写作结果并不是凭空生成，而是建立在会话上下文和任务类型识别基础上的定向产物。",
                "前端通过 WritingEntryHub 将写作助手与 PPT 生成入口清晰分开，避免用户在普通聊天界面中面对过多写作配置。写作助手负责围绕邮件、分析报告和营销文案等任务组织生成请求，而 PPT 入口则把用户导向独立的演示文稿工作流。一级入口加二级子流程的实现模式，有效平衡了统一工作台的一致性和复杂生成任务的操作深度。",
                "PPT 生成的后端实现集中在 presentation_router.py。该模块既支持本地 Presenton 服务，也支持同步、异步和有序流水线等多种生成路径，并维护了任务轮询、状态标准化、模板列表获取、模板导入、文件下载和代理访问等一整套接口。代码中还引入了运行时模型切换、单模型保活、模板冷却和线程池生成大纲等机制，表明 PPT 生成已经被视为一个高成本、需精细调度的独立子系统。",
                "值得注意的是，presentation_router.py 不仅完成了与 Presenton 的接口转发，还对模板元数据、内置模板目录、模板翻译文本和代理资源路径进行了工程化封装。这意味着系统希望让 PPT 生成不仅“能调通外部服务”，还要在本平台内形成稳定、可管理、可本地化的使用体验。再结合前端对任务状态展示和文件下载的支持，PPT 模块已经具备较为完整的产品形态。",
                "总体来看，报告、邮件与 PPT 生成实现将大模型的语言生成能力进一步转化为企业办公成果产出能力。它们与知识库、数据库、会议纪要和审单模块不是割裂关系，而是共享同一会话与上下文基础，并共同承担“把信息理解结果转化为可提交材料”的平台职责。",
            ],
        },
        {
            "title": "管理与运行支撑实现",
            "level": "section",
            "paragraphs": [
                "除了面向终端用户的前台能力外，企业智能办公平台还必须具备一定的组织治理和长期运行支撑能力。否则，即便核心功能在短期内表现良好，也难以在多用户、长周期和多资源环境中稳定使用。本项目在后台治理、运行时资源管理、安全边界控制和可维护性设计方面已经形成了较为明确的实现基础。",
                "从代码结构看，管理与运行支撑并不是附着在某个页面后的附加功能，而是以 admin_router.py、runtime_storage.py、认证与分享模块、中间件和若干自修复逻辑贯穿整个系统。下面将分别从角色治理与后台管理、运行时资源与安全维护两个方面进行说明。",
            ],
        },
        {
            "title": "角色治理与后台管理实现",
            "level": "subsection",
            "paragraphs": [
                "后台治理实现的核心入口在 admin_router.py。该模块通过 require_role 等方式对管理员、审单人员和知识库管理员等角色进行访问控制，并围绕用户资料、审单记录、知识库文档、规则配置和后台日志暴露一系列管理接口。这样的实现说明，系统并不是把后台能力简单藏在前端按钮之后，而是首先在后端建立了明确的角色边界和治理 API。",
                "在用户治理方面，后台模块支持查看和更新用户角色、状态和基础信息，并在需要时执行用户数据清理。admin_router.py 中可以删除指定用户关联的 session_titles、share_links、share_snapshots 和 documents 等数据，这意味着平台已经考虑到用户生命周期管理和组织级数据治理需求。对于企业场景而言，这种“账号治理 + 资产清理”能力是后台建设的重要组成部分。",
                "知识库管理方面，后台接口支持列出文档、审批文档、删除文档和重新为文档生成嵌入向量。管理员可以基于 metadata 和创建时间查看知识资源状态，并在发现异常时对文档进行重建索引或删除处理。由此可见，知识库并不是用户完全私有、无法治理的黑箱，而是具备后台审核与运维入口的可控资源。",
                "审单管理则是后台治理实现中的重点之一。admin_router.py 可以从数据库和本地状态中合并审单记录，提供筛选、搜索、详情、复核和规则更新接口，并记录管理员动作日志。这一实现使审单结果能够进入组织级复核流程，而不是只停留在前台单次展示中。结合 AuditAdminWorkspace 等前端界面，平台已形成较完整的审单后台工作链路。",
                "后台管理实现还具有一定的数据汇总和可观测能力。例如，系统会读取 admin_audit_logs 记录管理操作，整合审单作业数、文档数和用户资产情况，为管理员提供全局视角。这类实现虽然不直接创造业务结果，但对平台长期运行和问题定位至关重要。",
                "总体而言，角色治理与后台管理实现让系统具备了企业级平台应有的组织控制能力。通过明确角色边界、提供知识与审单管理入口、支持用户数据治理和保留管理日志，平台已经从单用户工具走向多用户、多角色协同的组织软件形态。",
            ],
        },
        {
            "title": "运行时资源、安全机制与可维护性实现",
            "level": "subsection",
            "paragraphs": [
                "运行时资源治理的核心实现集中在 runtime_storage.py。该模块统一定义了 .runtime 根目录下的 static、ocr、temp_audio、audit、cache 和 presentation 等多个子目录，并将短信令牌缓存、PPT 模板注册表和多类临时文件全部纳入统一路径体系。相较于把临时文件散落在项目根目录和历史静态目录中，当前实现明显更有利于后续运维和问题排查。",
                "runtime_storage.py 还实现了遗留目录迁移和定时清理机制。系统会把旧版 static/ocr、temp_audio、storage/audit 以及短信缓存文件自动迁移到新的统一目录，再根据 OCR、临时音频、审单和缓存资源各自的 TTL 周期执行清理。这样一来，项目在长期运行过程中不会因为临时文件不断堆积而导致磁盘和目录结构失控，体现出较强的工程维护意识。",
                "安全机制方面，系统通过多条链路共同划定边界。认证层负责访问令牌、刷新令牌和短信临时令牌管理；中间件层负责用户状态校验与非法会话拦截；数据库问答层通过只读 SQL 校验和表白名单控制防止越权查询；分享层通过 token_hash 和快照截断控制公开访问边界；后台管理层再通过角色检查限制高权限接口。多层安全机制共同作用，避免智能能力突破企业治理要求。",
                "为提高系统韧性，项目在多个关键模块中实现了故障隔离与回退逻辑。例如可选模块导入失败时主程序仍可启动其他功能，OCR 可在 PaddleOCR-VL 不可用时回退到标准引擎，会议转写改为进程内分片处理后也不再依赖外部队列基础设施，PPT 生成则在有序流水线失败时回退到上游异步接口。这些实现说明，系统在构建智能能力时并未忽视复杂环境下的异常处理问题。",
                "可维护性方面，项目整体采用了较为清晰的模块划分。前端按照页面、工作区、API 与通用组件组织，后端按照认证、聊天、文档、语音、OCR、审单、分享、PPT 和后台管理等能力拆分，很多模块还通过环境变量暴露运行参数，使部署环境能够在不修改核心代码的前提下完成策略调整。同时，历史表序列修复、索引自建和分享表自建等“自修复”实现，也降低了环境迁移和版本升级时的维护成本。",
                "因此，运行时资源、安全机制与可维护性实现构成了系统长期运行的底座。它们虽然不像聊天、知识库和审单那样直接呈现为前台功能，但正是这些底层实现保证了平台在面对多用户、多任务、多文件和多模块协同时仍能维持相对稳定、安全和可演进的运行状态。",
            ],
        },
        {
            "title": "本章小结",
            "level": "section",
            "paragraphs": [
                "本章围绕 Enterprise Intelligent Office Agent 2.0 的真实工程落地过程，对前端工作台、后端服务骨架、知识与数据能力、多模态处理、智能审单、写作与 PPT 生成，以及后台治理和运行时支撑机制进行了系统说明。通过分析可以看出，本项目并非简单围绕某个大模型接口搭建的问答演示系统，而是将统一交互入口、模块化服务组织、程序化约束和多源数据支撑结合起来，形成了一套具有企业办公适应性的智能平台实现方案。",
                "从实现效果上看，系统已经能够围绕知识检索、数据库问答、OCR、会议纪要、审单分析、报告邮件生成和 PPT 生成等多类办公任务提供端到端支持，并通过历史会话、分享快照、后台治理和运行时资源清理等机制补足组织级使用所需的留痕与管理能力。这说明本项目不仅在技术上完成了多模块集成，更在工程上初步建立了面向真实业务持续演进的基础。下一章将在此基础上进一步对系统测试与效果验证进行分析，以检验各项实现是否达到预期目标。",
            ],
        },
    ]

    return {
        "chapter_title": "系统实现",
        "toc": toc,
        "sections": sections,
    }


def _pick_source_path(repo_root: Path) -> Path:
    candidates = [
        repo_root / "artifacts" / "2026届毕业设计说明3.docx",
        repo_root / "artifacts" / "2026届毕业设计说明2_补全第四章.docx",
        repo_root / "artifacts" / "2026届毕业设计说明2.docx",
        repo_root / "artifacts" / "2026届毕业设计说明1_补全第三章.docx",
    ]
    last_error: Exception | None = None
    for candidate in candidates:
        if not candidate.exists():
            continue
        try:
            import zipfile

            with zipfile.ZipFile(candidate, "r") as zin:
                zin.read("word/document.xml")
            return candidate
        except Exception as exc:
            last_error = exc
            continue
    if last_error is not None:
        raise last_error
    raise FileNotFoundError("No available source docx found.")


def main() -> None:
    module = _load_common_module()
    repo_root = Path(__file__).resolve().parents[1]
    source_path = _pick_source_path(repo_root)
    output_path = repo_root / "artifacts" / "2026届毕业设计说明3_补全第五章.docx"
    fallback_output_path = repo_root / "artifacts" / "2026届毕业设计说明3_补全第五章_副本.docx"

    content = make_chapter5_content()

    with module.zipfile.ZipFile(source_path, "r") as zin:
        root = module.ET.fromstring(zin.read("word/document.xml"))
        body = root.find(module.qn("body"))
        if body is None:
            raise RuntimeError("word/document.xml missing body")

        paras = module.get_body_paragraphs(body)
        chapter_title_para = module.find_paragraph_by_exact_text(paras, "绪论")
        section_title_para = module.find_paragraph_by_exact_text(paras, "选题背景")
        subsection_title_para = module.find_paragraph_by_exact_text(paras, "国内研究现状")
        body_template_para = paras[paras.index(section_title_para) + 1]
        toc_chapter_template_para = next(
            para for para in paras if module.paragraph_text(para).startswith("1 绪论")
        )
        toc_section_template_para = next(
            para for para in paras if module.paragraph_text(para).startswith("1.1 ")
        )
        toc_subsection_template_para = next(
            para for para in paras if module.paragraph_text(para).startswith("1.2.1 ")
        )

        chapter_title_run = chapter_title_para.find(module.qn("r"))
        section_title_run = section_title_para.find(module.qn("r"))
        subsection_title_run = subsection_title_para.find(module.qn("r"))
        body_template_run = body_template_para.find(module.qn("r"))
        toc_chapter_run = toc_chapter_template_para.find(module.qn("r"))
        toc_section_run = toc_section_template_para.find(module.qn("r"))
        toc_subsection_run = toc_subsection_template_para.find(module.qn("r"))

        toc_anchor = chapter_title_para
        chapter_index = paras.index(chapter_title_para)
        for idx in range(chapter_index - 1, -1, -1):
            if module.paragraph_text(paras[idx]).strip():
                break
            toc_anchor = paras[idx]

        for level, text in content["toc"]:
            if level == "chapter":
                module.insert_paragraphs_before(
                    body,
                    toc_anchor,
                    toc_chapter_template_para,
                    toc_chapter_run,
                    [text],
                )
            elif level == "section":
                module.insert_paragraphs_before(
                    body,
                    toc_anchor,
                    toc_section_template_para,
                    toc_section_run,
                    [text],
                )
            else:
                module.insert_paragraphs_before(
                    body,
                    toc_anchor,
                    toc_subsection_template_para,
                    toc_subsection_run,
                    [text],
                )

        body_insertions: list[module.ET.Element] = []
        body_insertions.append(module.make_page_break_paragraph(body_template_para, body_template_run))

        chapter_para = module.deepcopy(chapter_title_para)
        module.set_paragraph_text(chapter_para, str(content["chapter_title"]), chapter_title_run)
        body_insertions.append(chapter_para)

        for item in content["sections"]:
            if item["level"] == "section":
                heading_para = module.deepcopy(section_title_para)
                heading_run = section_title_run
            else:
                heading_para = module.deepcopy(subsection_title_para)
                heading_run = subsection_title_run
            module.set_paragraph_text(heading_para, str(item["title"]), heading_run)
            body_insertions.append(heading_para)
            for text in item["paragraphs"]:
                para = module.deepcopy(body_template_para)
                module.set_paragraph_text(para, text, body_template_run)
                body_insertions.append(para)

        module.insert_before_sectpr(body, body_insertions)

        output_path.parent.mkdir(parents=True, exist_ok=True)
        target_path = output_path
        try:
            with module.zipfile.ZipFile(target_path, "w") as zout:
                for item in zin.infolist():
                    if item.filename != "word/document.xml":
                        zout.writestr(item, zin.read(item.filename))
                zout.writestr(
                    "word/document.xml",
                    module.ET.tostring(root, encoding="utf-8", xml_declaration=True),
                )
        except PermissionError:
            target_path = fallback_output_path
            with module.zipfile.ZipFile(target_path, "w") as zout:
                for item in zin.infolist():
                    if item.filename != "word/document.xml":
                        zout.writestr(item, zin.read(item.filename))
                zout.writestr(
                    "word/document.xml",
                    module.ET.tostring(root, encoding="utf-8", xml_declaration=True),
                )

    print(target_path)
    print(source_path)


if __name__ == "__main__":
    main()
