services:
  searxng:
    image: docker.io/searxng/searxng:latest
    container_name: imagine-searxng
    restart: unless-stopped
    ports:
      - "127.0.0.1:8888:8080"
    volumes:
      - ./settings.yml:/etc/searxng/settings.yml:ro
